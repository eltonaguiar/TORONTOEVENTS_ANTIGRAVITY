import ResearchClient from './ResearchClient';

export const metadata = {
    title: 'Stock Research | Predictive Alpha vs Supercomputers',
    description: 'Can a random person succeed in predicting stocks without a supercomputer? A deep dive into algorithmic research and validation methodologies.',
};

export default function ResearchPage() {
    return <ResearchClient />;
}
